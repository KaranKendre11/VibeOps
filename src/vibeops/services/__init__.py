"""UI-agnostic service functions used by the FastAPI layer (and unit tests).

These hold logic that used to live inside the now-removed Streamlit ``vibeops.ui.*`` screens —
e.g. Terraform-edit validation and the requirement-conversation decision logic.
"""
